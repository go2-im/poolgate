# ---- Go ----
/bin/
/dist/
*.exe
*.test
*.out
coverage.*

# ---- data & secrets (never commit) ----
/data/
*.db
*.db-wal
*.db-shm
*.sqlite
*.key
*.pem
.env
.env.*
poolgate.local.yaml
config.local.yaml
master.key

# ---- web frontend ----
/web/node_modules/
/web/dist/
/web/.vite/

# ---- embedded admin UI bundle ----
# The BUILT SPA (internal/webui/dist) IS committed so `go build` / `install.sh`
# embed the UI with no npm step (single-binary ethos). Rebuild it with
# `cd web && npm install && npm run build`.

# ---- editor / OS ----
.DS_Store
.idea/
.vscode/
*.swp

# built binary
/poolgate
cmd/poolgate/poolgate
